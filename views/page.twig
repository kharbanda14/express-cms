{% extends 'layouts/default.twig' %}

{% block metatags %}
    <meta name="title" content="{{title}}">

{% endblock %}

{% block body %}
    <div class="container">
        {% if post.featured_image %}
            <img class="featured-image" src="{{post.featured_image.large}}">
        {% endif %}
        <h1>
            {{ post.title }}
        </h1>
        <p>
            {{ post.content_rendered }}
        </p>
    </div>
{% endblock %}